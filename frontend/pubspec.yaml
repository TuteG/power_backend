name: powerprints
description: PowerPrints OS - Gestión Integral de Flota
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  shared_preferences: ^2.2.3
  provider: ^6.1.2
  fl_chart: ^0.68.0
  intl: ^0.19.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.jpg
